CC = gcc
JSON_C_DIR=/path/to/json_c/install

MIPS-CC = /user/include
FLAGS = -Wall -pthread
FLAGS += -I$(JSON_C_DIR)/include/json-c
LDFLAGS+= -L$(JSON_C_DIR)/lib -ljson-c

SRC = $(wildcard *.c)
OBJ = e2s

$(OBJ): $(SRC)
	$(MIPS-CC)  $(FLAGS) $(LDFLAGS) -o $@ $^

clean:
	rm -rf *~ *.o server e2s SOURCEINSIGHT
	
server:	$(SRC)

	$(CC) $(FLAGS) $(LDFLAGS) -o $@ $(SRC)
